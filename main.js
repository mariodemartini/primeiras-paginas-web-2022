const selecionaMenu = document.querySelectorAll('.botao');
const listaReceitas = document.querySelectorAll('.receita');

function clicaMenu(){
    for (indice=0; indice<selecionaMenu.length; indice++){
        selecionaMenu[indice]
    }
     
}

